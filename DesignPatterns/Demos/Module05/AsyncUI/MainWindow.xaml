<Window x:Class="AsyncUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Content="Get Data" FontSize="16" Margin="4" Padding="2" Click="OnGetData" />
        <TextBox Margin="4" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Grid.Row="1" x:Name="_data"/>
    </Grid>
</Window>
